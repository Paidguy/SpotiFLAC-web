version: "3.9"

services:
  spotiflac:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./downloads:/downloads
      - ./data:/data
    environment:
      - DOWNLOAD_PATH=/downloads
      - DATA_DIR=/data
      - PORT=8080
    restart: unless-stopped
